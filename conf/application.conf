# https://www.playframework.com/documentation/latest/Configuration

play {
  filters {
    hosts {
      allowed = ["now-playing-api-scala.herokuapp.com", "localhost:9000"]
    }

    cache {
      enabled=true
    }
  }
}